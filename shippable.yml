language: node_js

node_js:
   - "0.12"

env:
  global:
    - APP_NAME=opentrials-tracker

before_script:
  - npm install

script:
  - npm run build
  - npm run jscs
  - npm run test

after_script:
  - git push -f git@heroku.com:$APP_NAME.git master

notifications:
  email:
    - roll@respect31.com
